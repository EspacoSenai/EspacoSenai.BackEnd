package com.api.reserva.repository;

import com.api.reserva.entity.ReservaConvidados;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ReservaConvidadosRepository extends JpaRepository<ReservaConvidados, Long> {
}
